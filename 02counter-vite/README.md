# React + Vite

## 2nd project of  vite react 
This project includes the counter increment and decrement 
In this, we introduced the react hooks i.e. useState()
Hooks are the component that are used to re render the UI and updates in UI 
useState is a React Hook that allows a functional component to create and manage state (data that can change) and re-render the UI when that state changes.
